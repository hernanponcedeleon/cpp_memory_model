C coWW-sna-faddrlx-faddrlx
{ [x] = 0; }

P0 (int* x) {
  *x = 1;
  atomic_fetch_add_explicit(x, 1, memory_order_relaxed);
}


P1 (int* x) {
  atomic_fetch_add_explicit(x, 0, memory_order_relaxed);
}

~exists ([x]=0 \/ [x]=1)