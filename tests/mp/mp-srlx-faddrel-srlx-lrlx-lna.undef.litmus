C mp-srlx-faddrel-srlx-lrlx-lna-undef
{ [x] = 0; [y] = 0; }

P0 (int* x, int* y) {
  atomic_store_explicit(x, 1, memory_order_relaxed);
  atomic_fetch_add_explicit(y, 1, memory_order_release);
  atomic_store_explicit(y, 2, memory_order_relaxed);
}

P1 (int* x, int* y) {
  int a = atomic_load_explicit(y, memory_order_relaxed);
  if (a != 0) {
    int b = *x;
  }
}

forall(1:b=0 \/ 1:b=1)