C mp-sna-faddrel-faddrlx-lna-undef
{ [x] = 0; [y] = 0; }

P0 (int* x, int* y) {
  *x = 1;
  atomic_fetch_add_explicit(y, 1, memory_order_release);
}

P1 (int* x, int* y) {
  int a = atomic_fetch_add_explicit(y, 0, memory_order_relaxed);
  if (a != 0) {
    int b = *x;
  }
}


forall(1:b=0 \/ 1:b=1)