C mp-faddrlx-frel-faddrlx-lrlx-lacq-lna-undef
{ [x] = 0; [y] = 0; }

P0 (int* x, int* y) {
  atomic_fetch_add_explicit(x, 1, memory_order_relaxed);
  atomic_thread_fence(memory_order_release);
  atomic_fetch_add_explicit(y, 1, memory_order_relaxed);
}

P1 (int* x, int* y) {
  int a = atomic_load_explicit(y, memory_order_relaxed);
  int c = atomic_load_explicit(y, memory_order_acquire);
  if (a != 0) {
    int b = *x;
  }
}

P2 (int* y) {
  atomic_store_explicit(y, 0, memory_order_relaxed);
}

forall(1:b=0 \/ 1:b=1)