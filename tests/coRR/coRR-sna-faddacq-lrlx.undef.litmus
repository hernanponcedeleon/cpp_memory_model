C coRR-sna-faddacq-lrlx
{ [x] = 0; }

P0 (int* x) {
  *x = 1;
}

P1 (int* x) {
  int a = atomic_fetch_add_explicit(x, 0, memory_order_acquire);
  if (a == 1) {
    int b = atomic_load_explicit(x, memory_order_relaxed);
  }
}

~exists (1:a=1 /\ 1:b=0)