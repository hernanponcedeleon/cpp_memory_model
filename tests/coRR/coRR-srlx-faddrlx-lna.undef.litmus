C coRR-srlx-faddrlx-lna
{ [x] = 0; }

P0 (int* x) {
  atomic_store_explicit(x, 1, memory_order_relaxed);
}

P1 (int* x) {
  int a = atomic_fetch_add_explicit(x, 0, memory_order_relaxed);
  if (a == 1) {
    int b = *x;
  }
}

~exists (1:a=1 /\ 1:b=0)