C lb-lrlx-sna-lna-srlx-undef
{ [x] = 0; [y] = 0; }

P0 (int* x, int* y) {
  int a = atomic_load_explicit(y, memory_order_relaxed);
  *x = 1;
}

P1 (int* x, int* y) {
  int b = *x;
  if (b != 0) {
    atomic_store_explicit(y, 1, memory_order_relaxed);
  }
}

forall(0:a=0 \/ 0:a=1)