C lb-faddrlx-sna-lacq-faddrlx-undef
{ [x] = 0; [y] = 0; }

P0 (int* x, int* y) {
  int a = atomic_fetch_add_explicit(y, 0, memory_order_relaxed);
  *x = 1;
}

P1 (int* x, int* y) {
  int b = atomic_load_explicit(x, memory_order_acquire);
  if (b != 0) {
    atomic_fetch_add_explicit(y, 1, memory_order_relaxed);
  }
}

forall(0:a=0 \/ 0:a=1)