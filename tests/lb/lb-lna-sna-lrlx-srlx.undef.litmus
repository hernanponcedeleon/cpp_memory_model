C lb-lna-sna-lrlx-srlx-undef
{ [x] = 0; [y] = 0; }

P0 (int* x, int* y) {
  int a = *y;
  *x = 1;
}

P1 (int* x, int* y) {
  int b = atomic_load_explicit(x, memory_order_relaxed);
  if (b != 0) {
    atomic_store_explicit(y, 1, memory_order_relaxed);
  }
}

forall(0:a=0 \/ 0:a=1)