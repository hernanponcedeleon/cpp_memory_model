C lb-lna-faddrel-lna-srlx-undef
{ [x] = 0; [y] = 0; }

P0 (int* x, int* y) {
  int a = *y;
  atomic_fetch_add_explicit(x, 1, memory_order_release);
}

P1 (int* x, int* y) {
  int b = *x;
  if (b != 0) {
    atomic_store_explicit(y, 1, memory_order_relaxed);
  }
}

~exists(0:a=1)