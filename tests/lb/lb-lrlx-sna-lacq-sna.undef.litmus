C lb-lrlx-sna-lacq-sna-undef
{ [x] = 0; [y] = 0; }

P0 (int* x, int* y) {
  int a = atomic_load_explicit(y, memory_order_relaxed);
  *x = 1;
}

P1 (int* x, int* y) {
  int b = atomic_load_explicit(x, memory_order_acquire);
  if (b != 0) {
    *y = 1;
  }
}

forall(0:a=0 \/ 0:a=1)