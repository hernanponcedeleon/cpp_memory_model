C lb-faddrlx-sna-lna-srlx-undef
{ [x] = 0; [y] = 0; }

P0 (int* x, int* y) {
  int a = atomic_fetch_add_explicit(y, 0, memory_order_relaxed);
  *x = 1;
}

P1 (int* x, int* y) {
  int b = *x;
  if (b != 0) {
    atomic_store_explicit(y, 1, memory_order_relaxed);
  }
}

~exists(0:a=1)