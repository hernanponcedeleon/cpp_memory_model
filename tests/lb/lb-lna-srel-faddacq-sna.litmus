C lb-lna-srel-faddacq-sna
{ [x] = 0; [y] = 0; }

P0 (int* x, int* y) {
  int a = *y;
  atomic_store_explicit(x, 1, memory_order_release);
}

P1 (int* x, int* y) {
  int b = atomic_fetch_add_explicit(x, 0, memory_order_acquire);
  if (b != 0) {
    *y = 1;
  }
}

~exists(0:a=1)