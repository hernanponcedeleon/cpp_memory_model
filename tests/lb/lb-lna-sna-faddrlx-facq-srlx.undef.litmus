C lb-lna-sna-faddrlx-facq-srlx-undef
{ [x] = 0; [y] = 0; }

P0 (int* x, int* y) {
  int a = *y;
  *x = 1;
}

P1 (int* x, int* y) {
  int b = atomic_fetch_add_explicit(x, 0, memory_order_relaxed);
  atomic_thread_fence(memory_order_acquire);
  if (b != 0) {
    atomic_store_explicit(y, 1, memory_order_relaxed);
  }
}

forall(0:a=0 \/ 0:a=1)