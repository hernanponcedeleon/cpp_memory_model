C lb-faddrlx-faddrel-faddacq-faddrlx
{ [x] = 0; [y] = 0; }

P0 (int* x, int* y) {
  int a = atomic_fetch_add_explicit(y, 0, memory_order_relaxed);
  atomic_fetch_add_explicit(x, 1, memory_order_release);
}

P1 (int* x, int* y) {
  int b = atomic_fetch_add_explicit(x, 0, memory_order_acquire);
  if (b != 0) {
    atomic_fetch_add_explicit(y, 1, memory_order_relaxed);
  }
}

~exists(0:a=1)