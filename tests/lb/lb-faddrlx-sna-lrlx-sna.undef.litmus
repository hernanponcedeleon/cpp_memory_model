C lb-faddrlx-sna-lrlx-sna-undef
{ [x] = 0; [y] = 0; }

P0 (int* x, int* y) {
  int a = atomic_fetch_add_explicit(y, 0, memory_order_relaxed);
  *x = 1;
}

P1 (int* x, int* y) {
  int b = atomic_load_explicit(x, memory_order_relaxed);
  if (b != 0) {
    *y = 1;
  }
}

forall(0:a=0 \/ 0:a=1)