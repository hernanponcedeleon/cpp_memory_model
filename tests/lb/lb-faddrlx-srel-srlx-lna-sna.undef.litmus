C lb-faddrlx-srel-srlx-lna-sna-undef
{ [x] = 0; [y] = 0; }

P0 (int* x, int* y) {
  int a = atomic_fetch_add_explicit(y, 0, memory_order_relaxed);
  atomic_store_explicit(x, 1, memory_order_release);
  atomic_store_explicit(x, 2, memory_order_relaxed);
}

P1 (int* x, int* y) {
  int b = *x;
  if (b != 0) {
    *y = 1;
  }
}

forall(0:a=0 \/ 0:a=1)