C lb-lna-frel-srlx-lna-srlx-undef
{ [x] = 0; [y] = 0; }

P0 (int* x, int* y) {
  int a = *y;
  atomic_thread_fence(memory_order_release);
  atomic_store_explicit(x, 1, memory_order_relaxed);
}

P1 (int* x, int* y) {
  int b = *x;
  if (b != 0) {
    atomic_store_explicit(y, 1, memory_order_relaxed);
  }
}

~exists(0:a=1)