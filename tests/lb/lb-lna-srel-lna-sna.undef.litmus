C lb-lna-srel-lna-sna-undef
{ [x] = 0; [y] = 0; }

P0 (int* x, int* y) {
  int a = *y;
  atomic_store_explicit(x, 1, memory_order_release);
}

P1 (int* x, int* y) {
  int b = *x;
  if (b != 0) {
    *y = 1;
  }
}

~exists(0:a=1)