C lb-faddrlx-frel-faddrlx-faddrlx-lna-sna-undef
{ [x] = 0; [y] = 0; }

P0 (int* x, int* y) {
  int a = atomic_fetch_add_explicit(y, 0, memory_order_relaxed);
  atomic_thread_fence(memory_order_release);
  atomic_fetch_add_explicit(x, 1, memory_order_relaxed);
  atomic_fetch_add_explicit(x, 1, memory_order_relaxed);
}

P1 (int* x, int* y) {
  int b = *x;
  if (b != 0) {
    *y = 1;
  }
}

~exists(0:a=1)